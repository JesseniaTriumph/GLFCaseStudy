node_modules/
dist/
web/dist/
web/node_modules/
web/public/corpus-index.json
*.local
.env
.env.*
!.env.example
secrets/
.DS_Store

# transformers.js model cache
.cache/
**/.cache/

# runtime feedback capture
eval/feedback.jsonl
dist/raw/

# the deck-builder's deps (run `npm --prefix deliverables install` + `node deliverables/build_deck.cjs` to rebuild the pptx)
deliverables/node_modules/
deliverables/package-lock.json

# --- secrets (added by cleanup pass) ---
.env
.env.local
.env.*.local
.env.*
!.env.example
!.env.*.example
*.pem
*.key
*_service_account*.json
*serviceAccount*.json
credentials.json
secrets.json
*.p12
